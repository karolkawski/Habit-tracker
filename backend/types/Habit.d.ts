export type Frequency = {
    frequency: {
        days: { [key: string]: boolean };
      };
  }

  export type HabitType ={
    id: string;
    name: string;
    // Add other properties as needed
  }